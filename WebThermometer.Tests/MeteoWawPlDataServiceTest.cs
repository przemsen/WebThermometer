using System;
using NUnit.Framework;
using WebThermometer;
using System.Text.RegularExpressions;

namespace WebThermometer.Tests
{
    [TestFixture]
    public class MeteoWawPlDataServiceTest
    {
        [Test]
        public void ReturnsValidTemperature()
        {
            //var service = new MeteoWawPlDataService();
            //var result = (new Regex(@"\d+,\d+")).IsMatch(service.GetValue1());
            //Assert.IsTrue(result);
        }
    }
}
